# Documentation Drift Report
Generated: 2026-04-17T07:51:54Z
Direction: code-to-docs
Scope: full repo

## Summary

| Severity | Count | Auto-fixable |
|----------|-------|--------------|
| HIGH     | 19    | 19           |
| MEDIUM   | 7     | 4            |
| LOW      | 0     | 0            |

## HIGH Severity Findings (auto-fixed)

### CLI Domain

- [UNDOCUMENTED] `aiwg mcp profile add/list/show/edit/remove/import/export/init-presets` — 8 subcommands with no entry in docs/cli-reference.md  
  Fix: added full `#### mcp profile` section including subcommand table, preset profiles reference table, and examples

- [UNDOCUMENTED] `aiwg mcp inject --profile <name>` — no docs entry  
  Fix: added to `#### mcp inject` Options table with explanation

- [UNDOCUMENTED] `aiwg mcp inject --ephemeral` — no docs entry  
  Fix: added with supported/unsupported provider notes

- [UNDOCUMENTED] `aiwg mcp inject --out <path>` — no docs entry  
  Fix: added to Options

- [UNDOCUMENTED] `aiwg session --profile <name>` — no docs entry  
  Fix: added to session Options with ephemeral-by-default explanation

- [UNDOCUMENTED] `aiwg session --persist` — no docs entry  
  Fix: added to session Options

- [UNDOCUMENTED] `aiwg mcp add <name>` — no docs entry  
  Fix: added full `#### mcp add` section with all options

- [UNDOCUMENTED] `aiwg mcp remove <name>` — no docs entry  
  Fix: added `#### mcp remove` section

- [UNDOCUMENTED] `aiwg mcp update <name>` — no docs entry  
  Fix: added `#### mcp update` section

- [UNDOCUMENTED] `aiwg mcp list` — no docs entry  
  Fix: added `#### mcp list` section

- [UNDOCUMENTED] `aiwg mcp inject` (base command) — referenced but not documented  
  Fix: added full `#### mcp inject` section with all flags

### Config Domain

- [UNDOCUMENTED] `PRESET_PROFILES` (minimal/dev/ops/research/incident/full) — no reference table in docs  
  Fix: added preset profiles table in `mcp profile` section

- [UNDOCUMENTED] `~/.aiwg/mcp-profiles.json` schema — storage path and format not documented  
  Fix: added "Profile storage" note in `mcp profile` section

- [UNDOCUMENTED] `~/.codex/roles-runtime/<profile>/` Codex runtime home structure  
  Fix: added "Codex runtime homes" explanation in `mcp profile` section

### Summary Table

- [STALE] `| **MCP** | 1 | mcp (3 subcommands) |` — 3 subcommands is wrong (now 9)  
  Fix: updated to `mcp (serve, install, info, add, remove, update, list, inject, profile)`

### CLAUDE.md

- [UNDOCUMENTED] MCP registry management commands missing from quick reference  
  Fix: added mcp add/remove/list/inject/profile commands

## MEDIUM Severity Findings (manual review recommended)

- `McpProfileRegistry` class API — no API reference docs generated. The class is well-documented via JSDoc in source; dedicated API docs would help advanced users building integrations.
- `SharedStatePolicy` struct — the symlink policy is documented inline but a configuration reference doc would help operators customizing Codex runtime home behavior.
- `McpProfile.providerOverrides.toolDeny/toolAllow` — capability not described anywhere in user-facing docs. Currently only used in the `dev` preset (Gitea destructive tool deny for Codex).
- Per-profile OAuth token isolation flow diagram missing — the Codex runtime-home adapter pattern is novel and would benefit from a sequence diagram.

## Files Modified

| File | Findings | Auto-fixed |
|------|----------|------------|
| docs/cli-reference.md | 18 | 18 |
| CLAUDE.md | 1 | 1 |

## Cross-Reference Validation

- No broken anchor links introduced in new content
- Code examples in new sections use valid, current CLI syntax matching source code
- Version strings: not applicable (no version references in new content)
